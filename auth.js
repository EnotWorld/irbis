// auth.js

// Пример простой проверки учетных данных (замените на реальную аутентификацию)
function authenticate(username, password) {
  return (username === 'admin' && password === 'password');
}

module.exports = {
  authenticate
};
